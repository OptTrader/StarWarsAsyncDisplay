# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'StarWarsAsyncDisplay' do
    use_frameworks!

    pod 'AsyncDisplayKit', '>= 2.0'
    pod 'Alamofire', '~> 4.0'
    pod 'SwiftyJSON'

  target 'StarWarsAsyncDisplayTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
